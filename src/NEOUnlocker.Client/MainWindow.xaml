<Window x:Class="NEOUnlocker.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NEOUnlocker Pro - Secure Firmware Flash" 
        Height="600" 
        Width="800"
        MinHeight="500"
        MinWidth="700"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="NEOUnlocker Pro" 
                       FontSize="24" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Secure Firmware Flash System" 
                       FontSize="14" 
                       Foreground="Gray"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Device Selection -->
        <GroupBox Grid.Row="1" Header="Device Configuration" Padding="10">
            <StackPanel>
                <TextBlock Text="Select Device Type:" FontWeight="SemiBold"/>
                <ComboBox x:Name="DeviceTypeComboBox" 
                          SelectedIndex="0"
                          IsEnabled="{Binding IsNotFlashing}">
                    <ComboBoxItem Content="MTK6580"/>
                    <ComboBoxItem Content="Qualcomm8937"/>
                </ComboBox>
            </StackPanel>
        </GroupBox>

        <!-- Hardware ID Display -->
        <GroupBox Grid.Row="2" Header="Device Information" Padding="10">
            <StackPanel>
                <TextBlock Text="Hardware ID:" FontWeight="SemiBold"/>
                <TextBox x:Name="HwidTextBox" 
                         IsReadOnly="True" 
                         Background="#F0F0F0"
                         FontFamily="Consolas"
                         Text="Loading..."/>
            </StackPanel>
        </GroupBox>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0">
            <Button x:Name="FlashButton" 
                    Content="Start Flash" 
                    Width="150"
                    Height="40"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Click="FlashButton_Click"
                    IsEnabled="{Binding IsNotFlashing}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#4CAF50"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="Margin" Value="5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="5"
                                            BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45a049"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            
            <Button x:Name="CancelButton" 
                    Content="Cancel" 
                    Width="150"
                    Height="40"
                    FontSize="16"
                    Click="CancelButton_Click"
                    IsEnabled="{Binding IsFlashing}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#f44336"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="Margin" Value="5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="5"
                                            BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#da190b"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <!-- Progress Log -->
        <GroupBox Grid.Row="4" Header="Progress Log" Padding="10" Margin="0,20,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="ProgressTextBox"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         FontSize="12"
                         Background="#1E1E1E"
                         Foreground="#D4D4D4"
                         BorderThickness="0"
                         Text="Ready. Select device type and click 'Start Flash' to begin."/>
            </ScrollViewer>
        </GroupBox>

        <!-- Status Bar -->
        <Border Grid.Row="5" 
                Background="#F0F0F0" 
                Padding="10"
                Margin="0,10,0,0"
                CornerRadius="3">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Status: " FontWeight="SemiBold"/>
                <TextBlock x:Name="StatusTextBlock" Text="Ready"/>
                <TextBlock Text=" | " Margin="10,0"/>
                <TextBlock Text="Server: " FontWeight="SemiBold"/>
                <TextBlock x:Name="ServerStatusTextBlock" Text="Not Connected" Foreground="Orange"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
